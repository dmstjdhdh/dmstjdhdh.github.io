import React from 'react';
import styled from 'styled-components';

const BlogContainer = styled.div`
  padding: 20px;
  max-width: 800px;
  margin: 0 auto;
`;

const Blog = () => {
  return (
    <BlogContainer>
      <div></div>
    </BlogContainer>
  );
};

export default Blog;
